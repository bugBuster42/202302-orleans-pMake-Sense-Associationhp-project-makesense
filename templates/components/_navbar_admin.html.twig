<div class="container">
	<nav class="navbar navbar-expand-lg navbar-transparent bg-transparent">
		<div class="container-fluid mx-3">
			<a class="navbar-brand fs-2 fw-bold text-light mx-0" href="{{ path('home_page') }}"><img src="{{ asset('build/images/logo-dark.png') }}" alt="makesense" width="160px"></a>
			<button class="navbar-toggler  border border-primary p-1" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<img src="{{ asset('build/images/menu.svg') }}" alt="menu" width="50px">
			</button>
			<div class="collapse navbar-collapse justify-content-end fw-semibold" id="navbarNav">
				<ul class="navbar-nav align-items-center gap-3">
					<li class="nav-item">
						<a class="link nav-link fw-bold text-primary px-3" href="{{ path('admin_home') }}">Accueil</a>
					</li>
					<li class="nav-item">
						<a class="link nav-link fw-bold text-primary px-3" href="{{ path('admin_user_index') }}">Utilisateurs</a>
					</li>
					<li class="nav-item">
						<a class="link nav-link text-primary fw-bold px-3" href="{{ path('admin_category_index') }}">Catégories</a>
					</li>
					<li class="nav-item text-primary">
						{% if app.user %}

							<a class="profile" href="{{ path('app_profile', {'id': app.user.id}) }}">
								<img class="img-profile rounded-circle border border-2 border-primary" src="{{ asset('uploads/avatar/' ~ app.user.avatar) }}" alt="profil">
							</a>

						</li>

						<div class="dropdown profile">
							<button class="link btn-profile p-1 dropdown-toggle text-primary bg-tertiary fw-bold border-tertiary" type="button" data-bs-toggle="dropdown" aria-expanded="false">
								{{ app.user.firstname|first}}
								{{ app.user.lastname|first}}
							</button>
							<ul class="dropdown-menu bg-tertiary border border-tertiary">
								<li>
									<a class="dropdown-item fw-bold" href="{{ path('app_profile', {'id': app.user.id}) }}">Profil</a>

								</li>
								<li class="nav-item text-primary">
									<a class="dropdown-item fw-bold" href="{{ path('app_logout') }}">Déconnexion</a>
								</li>
							</ul>
						</div>

					{% else %}
						<a class="link nav-link fw-bold text-primary px-3" href="{{ path('app_login') }}">Connexion</a>
					{% endif %}
				</li>

			</ul>
		</div>
	</nav>
</div>
