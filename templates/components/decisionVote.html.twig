<div{{attributes}}>
	{% if hasVoted == false %}
		{% if decision.status == 'Prise de décision commencée' and app.user != decision.user or decision.status == 'Décision définitive' and app.user != decision.user %}
			<button type="button" class="" data-action="live#action" data-action-name="voted(direction=up)">
				Pour
				<i class="bi bi-check-square-fill"></i>
			</button>
			<button type="button" class="" data-action="live#action" data-action-name="voted(direction=down)">
				Vote contre
				<i class="bi bi-x-square-fill"></i>

			</button>
		{% endif %}
	{% endif %}
	<p>vote pour
		{{ this.decision.votes|filter(vote => vote.voting == 1)|length }}</p>
	<p>Vote contre
		{{ this.decision.votes|filter(vote => vote.voting == -1)|length }}</p>
</div>
