{% extends 'base.html.twig' %}

{% block title %}Decision{% endblock %}

{% block body %}
    <h1>Decision</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ decision.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ decision.title }}</td>
            </tr>
            <tr>
                <th>LimitDate</th>
                <td>{{ decision.limitDate ? decision.limitDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ decision.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_decision_index') }}">back to list</a>

    <a href="{{ path('app_decision_edit', {'id': decision.id}) }}">edit</a>

    {{ include('decision/_delete_form.html.twig') }}
{% endblock %}
