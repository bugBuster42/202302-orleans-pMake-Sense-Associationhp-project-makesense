<form method="post" action="{{ path('app_decision_delete', {'id': decision.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ decision.id) }}">
    <div class="text-end">
    <button class="btn btn-light text-primary my-2 border border-secondary rounded-pill">Delete</button>
    </div>
</form>
